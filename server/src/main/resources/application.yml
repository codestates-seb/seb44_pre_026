spring:
  h2:
    console:
      enabled: true
      path: /h2 # h2 ?? ?? ex)localhost/h2
  datasource:
    url: jdbc:h2:mem:test #h2 ?? url
  jpa:
    hibernate:
      ddl-auto: create #jpa? ???? DB? ???? ??
    show-sql: true #jpa API? ??? ???? SQL??? ??? ????
logging:
  level:
    org:
      springframework:
        orm:
          jpa: DEBUG